<h3><%= t('event.events')%></h3>
<table class="table table-striped"><tr>
  <th><%= t('event.name')%></th>
	<th><%= t('event.begin')%></th>
	<th><%= t('event.end')%></th>
</tr>
<% @events.each do |e| %>
  <tr>
    <td><%= link_to e.code, e %></td>
		<td><%= e.begindate%></td>
		<td><%= e.enddate%></td>
</tr>
<%end%>
</table>
